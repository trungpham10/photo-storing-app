<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Page</title>
</head>

<body>
    <img src="<%= moment.img %>" width="500" height="500" />
    <h3><%= moment.title %></h3>
    <p><%= moment.description %></p>
    <button><a href="/<%= moment._id %>/edit">Edit</a></button>
    <form action="/<%= moment.id %>?_method=DELETE" method="POST">
        <input type="submit" value="Delete">
    </form>
</body>

</html>